<h1>Booking Details</h1>
<p><strong>Service:</strong> <%= @booking.service&.name || 'N/A' %></p>
<p><strong>Psychologist:</strong> <%= @booking.psychologist_profile&.full_name || 'N/A' %></p>
<p><strong>Client:</strong> <%= @booking.client_profile&.full_name || @booking.internal_client_profile&.full_name || 'N/A' %></p>
<p><strong>Start Time:</strong> 
  <span class="utc-date-time-range" 
        data-start-utc="<%= @booking.start_time.utc.iso8601 %>" 
        data-end-utc="<%= @booking.start_time.utc.iso8601 %>">
    Loading time...
  </span>
</p>
<p><strong>End Time:</strong> 
  <span class="utc-date-time-range" 
        data-start-utc="<%= @booking.end_time&.utc&.iso8601 %>" 
        data-end-utc="<%= @booking.end_time&.utc&.iso8601 %>">
    Loading time...
  </span>
</p>
<p><strong>Duration:</strong> <%= @booking.service&.duration_minutes ? "#{@booking.service.duration_minutes} minutes" : number_with_precision((@booking.end_time - @booking.start_time) / 60, precision: 0) + ' minutes' %></p>
<p><strong>Status:</strong> <%= @booking.status %></p>
<p><strong>Debug:</strong> status=<%= @booking.status %>, created_by=<%= @booking.created_by %>, token=<%= @booking.confirmation_token %></p>

<% if @booking.pending? && @booking.created_by == 'psychologist' %>
  <p><strong>Share this link with the client to confirm the booking:</strong> 
    <a href="<%= confirm_booking_url(@booking, token: @booking.confirmation_token) %>">
      <%= confirm_booking_url(@booking, token: @booking.confirmation_token) %>
    </a>
  </p>
  <p><strong>Share via:</strong></p>
  <p>
    <a href="https://t.me/share/url?url=<%= CGI.escape(confirm_booking_url(@booking, token: @booking.confirmation_token)) %>&text=Please%20confirm%20your%20booking%20for%20<%= CGI.escape(@booking.service&.name || 'N/A') %>%20with%20<%= CGI.escape(@booking.psychologist_profile&.full_name || 'N/A') %>"
       class="btn btn-primary" target="_blank">
      Share to Telegram
    </a>
  </p>
  <p>
    <a href="https://api.whatsapp.com/send?text=Please%20confirm%20your%20booking%20for%20<%= CGI.escape(@booking.service&.name || 'N/A') %>%20with%20<%= CGI.escape(@booking.psychologist_profile&.full_name || 'N/A') %>:%20<%= CGI.escape(confirm_booking_url(@booking, token: @booking.confirmation_token)) %>"
       class="btn btn-success" target="_blank">
      Share to WhatsApp
    </a>
  </p>
<% end %>