


<h2><b>Sign up</b></h2>
<br>
<p>
Пожалуйстя проверьте спам папку. Gmail хорошо работает. 

Есть проблемы с Мейл ру. Если что можете использовать temp-mail.org, для временного имейла. 

если у вас будут проблемы в контактах есть наш телеграм и имейл. 
</p>
<br>
<br>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>


<div class="field mb-3">
   <span>Я </span>  <%= f.select :role,
        options_for_select([ ['Психолог/Терапевт', 'psychologist'],['Клиент', 'client']], selected: resource.role),
        {},
        { class: "form-select" } %> 

  </div>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
